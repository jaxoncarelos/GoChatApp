A small Go TCP chat app to get more familiar with concurrency.

# Usage:
`go run . <address> <port>` While in ./GoChatApp
`go run . <address> <port> <username>` while in ./GoCat while the TCP server is running.
